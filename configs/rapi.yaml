package:
  name: "{{.Package.Name}}"
  author: "{{.Package.Author}}"
  url: "{{.Package.URL}}"
  version: "{{.Package.Version}}"
  description: "{{.Package.Description}}"
  license: "{{.Package.License}}"
  recipe: "{{.Package.Recipe}}"
rapi:
  version: "{{.Rapi.RapiVersion}}"

dependencies:
