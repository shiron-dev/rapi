package:
  name: "{{.Name}}"
  author: "{{.Author}}"
  url: "{{.URL}}"
  version: "{{.Version}}"
  description: "{{.Description}}"
  license: "{{.License}}"
rapi:
  version: "{{.RapiVersion}}"

dependencies:
